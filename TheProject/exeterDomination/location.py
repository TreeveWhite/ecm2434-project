

def posInSquare(top : int, left : int, bottom : int, right : int, posLat : int, posLong : int) -> bool:
    # inArea = False
    # # Check if the position longitude is beteen the leftmost and rightmost.
    # if ((left <= posLong) and (right >= posLong)) or ((left < 0 and right >= 0) and (left <= )):
    #     # Check if the position latitude is between the top and bottom.
    #     if (top >= posLat) and (bottom <= posLat):
    #         inArea = True
    
    # return inArea

    pass


# This works for when not points around 0 swapover.